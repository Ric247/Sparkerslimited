The site now ships with an inline SVG logo built into the React app,
so no external image file is required to render the header/footer logo.

Optional: if you want to use a raster version of the logo (for
Open Graph previews, email signatures, etc.), drop a sparkers_logo.png
file in this folder and reference it as /sparkers_logo.png.